<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="whatever.gamepool">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/game_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo">
        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Holo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="whatever.game2048.Game2048Activity"
            android:label="@string/game2048_name"
            android:screenOrientation="portrait" />
        <activity
            android:name="whatever.game2048.Menu2048Activity"
            android:label="@string/game2048_name"
            android:screenOrientation="portrait" />
        <activity
            android:name="whatever.gameRace.MenuRaceActivity"
            android:label="@string/game_race_name"
            android:screenOrientation="portrait" />
        <activity android:name="whatever.gameRace.GameRaceActivity"
            android:label="Race play"
            android:screenOrientation="portrait" />
        <activity
            android:name="whatever.Fifteen.Menu15Activity"
            android:label="@string/game_race_name"
            android:screenOrientation="portrait" />
        <activity android:name="whatever.Fifteen.Game15Activity"
            android:label="Play 15"
            android:screenOrientation="portrait" />
    </application>

</manifest>
